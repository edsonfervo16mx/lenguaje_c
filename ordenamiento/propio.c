#include <stdio.h>
int main()
{
    int cantidad = 0;
    char arreglo[];
    printf("Ingresar la cantidad de numeros a ordenar ");
    scanf("%d", &cantidad);
    printf("Cantidad: %d \n", cantidad);
return 0;
}